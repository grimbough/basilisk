#!/bin/sh

"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" --slave --quiet -e 'basilisk::setupVirtualEnv("env1", "pandas==0.25.1", pkgname="son.of.basilisk")'
"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" --slave --quiet -e 'basilisk::setupVirtualEnv("env2", "scikit-learn==0.21.0", pkgname="son.of.basilisk")'
